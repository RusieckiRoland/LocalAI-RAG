YAMLpipeline:
  name: shannon

  settings:
    entry_step_id: set_response
    snapshot_set_id: "nopCommerce_4-60_4-90"

  steps:
    - id: set_response
      action: set_variables
      rules:
        - set: last_model_response
          value: "Testowa odopowiedź dla piplinu Shanon"
      next: finalize

    - id: finalize
      action: finalize
      end: true
