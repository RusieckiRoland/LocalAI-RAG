YAMLpipeline:
  name: ada

  settings:
    entry_step_id: set_response
    snapshot_set_id: ""

  steps:
    - id: set_response
      action: set_variables
      rules:
        - set: last_model_response
          value: "Test odpowiedź od Ada"
      next: finalize

    - id: finalize
      action: finalize
      end: true
