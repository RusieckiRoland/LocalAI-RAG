# File: pipelines/rejewski.yaml
YAMLpipeline:
  name: marian_rejewski_code_analysis_nopCommerce_develop

  # Relative paths allowed (resolved relative to this YAML file)
  extends: ./base/marian_rejewski_code_analysis_base.yaml

  settings:
    repository: "nopCommerce"
    active_index: "2025-12-14_develop"

    # optional overrides (keep only if you want them different than base)
    entry_step_id: maybe_translate_in
    model_language: en
